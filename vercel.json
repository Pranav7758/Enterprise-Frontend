{
  "buildCommand": "pnpm --filter @workspace/expenseflow run build && cp -r artifacts/expenseflow/dist dist",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
